# Vertical slice (notebook 01). Heavier deps (torch, librosa, parselmouth) arrive
# with later notebooks — keep the first install light and fast.
faster-whisper>=1.0     # CTranslate2 Whisper: ~8-10x realtime on M-series CPU, no torch needed yet
pandas>=2.0
scikit-learn>=1.4
scipy>=1.11
matplotlib>=3.8
soundfile>=0.12         # read flac headers for durations
jupyterlab>=4.0
ipykernel
nbconvert               # headless notebook execution

# Notebook 02: WER + acoustic/prosody features
jiwer>=4.0              # WER / word alignment against gold STM transcripts
praat-parselmouth>=0.4  # Praat bindings: pitch (F0) and intensity contours

# Notebook 03: full dev set, non-linear models
torch>=2.0              # small MLP regressor (speechlab.scoring_models)
pytest>=8.0             # test suite (tests/, synthetic fixtures only)

# Apple Silicon LoRA fine-tuning (Apple Silicon only; rest of lab runs anywhere)
mlx>=0.32
mlx-lm>=0.31

# Direct imports (also arrive transitively via mlx-lm, declared explicitly here)
transformers>=4.44      # tokenizer-only parity check (llm_scorer.py)
pyyaml>=6.0             # per-fold training config writer (finetune_lora.py)
