# Vertical slice (notebook 01). Heavier deps (torch, librosa, parselmouth) arrive
# with later notebooks — keep the first install light and fast.
faster-whisper>=1.0     # CTranslate2 Whisper: ~8-10x realtime on M-series CPU, no torch needed yet
pandas>=2.0
scikit-learn>=1.4
scipy>=1.11
matplotlib>=3.8
soundfile>=0.12         # read flac headers for durations
jupyterlab>=4.0
ipykernel
nbconvert               # headless notebook execution

# Notebook 02: WER + acoustic/prosody features
jiwer>=4.0              # WER / word alignment against gold STM transcripts
praat-parselmouth>=0.4  # Praat bindings: pitch (F0) and intensity contours
